spring:
  datasource:
    jdbc-url: ${spring.datasource.url}

one:
  datasource:
    jdbc-url: jdbc:h2:mem:one;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
two:
  datasource:
    jdbc-url: jdbc:h2:mem:two;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
